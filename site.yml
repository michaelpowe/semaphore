---
- hosts: homelab
  tasks:
    - name: This command will change the working directory to somedir/
      ansible.builtin.shell:
        cmd: touch ansible.txt && echo "hello" > ansible.txt
        chdir: /tmp
